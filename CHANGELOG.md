# 版本变更日志（CHANGELOG）

## v10.28.57 — 性能专项 + 隐私合规（当前版本）
- **性能**：看板 `rows` 改为列式存储 + 字典编码 + 位打包，HTML 由 28.8MB 降至 ~730KB（gzip 143KB）；解码零误差、12 万行解码 < 1s。
- **性能**：服务端 `serve.py` 增加 gzip 压缩与压缩结果缓存；`build_lists.py` 增加 schema 磁盘缓存（重复同步不再 30+ 次查列）。
- **性能**：`assign.py` 全程只读一次 `records.json`；XLSX 改用 `append` 批量写（12 万行约 19s）。
- **隐私合规**：新增 `desensitize_data.py`，将真实数据脱敏为「可公开」版本（手机号掩码、员工匿名、街道/社区清空），仓库内置脱敏示例数据，数据库凭证永不入库。
- **归属地**：保留三级兜底（数据自带 → 离线字典 642 条 → 浏览器端在线 ip138/360）。
- **交互**：搜索 chip + 推荐；接待人/标签筛选；布局重排（接待时间区域加大）。

## v10.28.56 — 同网段 / 归属地 / 搜索体验
- 修复同网段无法访问（`open_firewall.bat` 放行 + `?token=` 复制链接）。
- 修复手机号归属地丢失（离线字典扩容 + 在线 API 兜底）。
- 搜索后支持点击选择联系人；不再直接展示全员名单。

## v10.28.54 — 稳定性
- 引入 `_safe_exec` 自动重连 + 重试，根治 `pymysql.InterfaceError(0,'')`（MySQL wait_timeout 踢线）。

## v10.28.53 — 回访排班
- 修复回访排班加载失败、无法生成回访名单的问题（端点超时隔离）。

## v10.28.4x — 早期能力沉淀
- 低频用户识别模型、欠租/退订状态机、促成漏斗、接待人排行、按日/周/月统计。
- 离线 Mock 数据源（`mock_db.py`），无库也能跑通演示。

## v10.10 — 项目起点
- 首个可用的数据同步 + 看板原型。
